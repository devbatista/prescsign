Olá,

Seu documento digital assinado está pronto.

Código: <%= @document.code %>
Tipo: <%= @document.kind %>

Baixe seu documento (link seguro, expira em <%= @download_ttl_days %> dias):
<%= @download_url %>

Para validar a autenticidade a qualquer momento, pelo código:
<%= @validation_url %>
