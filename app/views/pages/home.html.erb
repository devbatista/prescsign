<% content_for :title, "PrescSign" %>

<div class="rounded-2xl border border-gray-200 bg-white p-8 shadow-sm">
  <span class="inline-flex items-center rounded-full bg-emerald-100 px-3 py-1 text-xs font-medium text-emerald-700">
    Fase 1 · camada web ativa
  </span>

  <h1 class="mt-4 text-3xl font-bold tracking-tight">PrescSign</h1>
  <p class="mt-2 text-gray-600">
    Migração para Rails server-rendered (ERB) em andamento. Esta página confirma que
    o pipeline de views, Tailwind e importmap está funcionando.
  </p>

  <dl class="mt-6 grid grid-cols-2 gap-4 text-sm">
    <div class="rounded-lg bg-gray-50 p-4">
      <dt class="text-gray-500">Camada</dt>
      <dd class="mt-1 font-semibold">Rails MVC · ERB</dd>
    </div>
    <div class="rounded-lg bg-gray-50 p-4">
      <dt class="text-gray-500">Estilo</dt>
      <dd class="mt-1 font-semibold">Tailwind CSS v4</dd>
    </div>
  </dl>
</div>
